import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  // Define your initial state here
};

export const sliceTemplate = createSlice({
  name: "sliceTemplate",
  initialState,
  reducers: {
    // Define your reducer functions here
  },
});

export const { /* Add your action creators here */ } = sliceTemplate.actions;